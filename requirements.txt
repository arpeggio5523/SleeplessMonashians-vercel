# --- pipeline (required) ---
pdfplumber>=0.11          # PDF text extraction (fallback when poppler absent)
python-docx>=1.1          # DOCX attachments
openpyxl>=3.1             # XLSX attachments

# --- Gemini fallback (required for --llm) ---
google-genai>=1.0

# --- tests ---
pytest>=8.0

# --- dev tools ---
reportlab>=4.0            # ONLY needed by the organisers' generate.py (sweep.py)

# --- API service ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9
